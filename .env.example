# DOCKER
DOCKER_SOCKED_PATH=/var/run/docker.sock

# SYSTEM ENV
SYSTEM_TITLE=ComputingTutor
SYSTEM_GIT_NAME="${SYSTEM_TITLE} API"
SYSTEM_GIT_EMAIL=noreply@domain.com
SYSTEM_DEPLOYMENT_PATH=/tmp/computor

EXECUTION_BACKEND_API_URL=http://172.17.0.1:8000
EXECUTION_BACKEND_API_USER=admin
EXECUTION_BACKEND_API_PASSWORD=admin

API_LOCAL_STORAGE_DIR=${SYSTEM_DEPLOYMENT_PATH}/execution-backend/shared

TOKEN_SECRET="CHANGE_THIS_SECRET_KEY"

# PostgreSQL Configuration
POSTGRES_HOST=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres_secret
POSTGRES_DB=codeability
POSTGRES_PORT=5432
POSTGRES_URL=${POSTGRES_HOST}:${POSTGRES_PORT}

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis_password

# Keycloak Configuration
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin_password
KEYCLOAK_SERVER_URL=http://localhost:8180
KEYCLOAK_REALM=computor
KEYCLOAK_CLIENT_ID=computor-backend
KEYCLOAK_CLIENT_SECRET=CHANGE_THIS_CLIENT_SECRET

# Prefect Configuration
PREFECT_API_URL=http://prefect:4200/api
PREFECT_POSTGRES_USER=postgres
PREFECT_POSTGRES_PASSWORD=postgres_secret
PREFECT_POSTGRES_DB=prefect
PREFECT_API_DATABASE_CONNECTION_URL=postgresql+asyncpg://${PREFECT_POSTGRES_USER}:${PREFECT_POSTGRES_PASSWORD}@prefect_db:5432/${PREFECT_POSTGRES_DB}

# Flower Configuration (Celery monitoring)
FLOWER_BASIC_AUTH=admin:flower123

DEBUG_MODE=development